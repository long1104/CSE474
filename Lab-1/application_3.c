#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*int main()
{
    int delay;

    scanf("%i", &delay);

    while (1) {
        f1Data(delay);
        f2Clear(delay);
    }
}

void printDelay(int delay) {
    clock_t start = clock();

    while (clock() - start < delay * CLOCKS_PER_SEC) {
    }
}

void f1Data(unsigned long delay) {
    printf("A B C D");
    printDelay(delay);
    system("cls");
}*/
